import { Games, Rooms, User, Winner } from '../constants/types';

export const users: User[] = [];
export const rooms: Rooms[] = [];
export const games: Games = {};
export const winners: Winner[] = [];
